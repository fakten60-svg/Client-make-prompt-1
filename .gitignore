# ── Build artifacts ──────────────────────────────────────────────
/build/
/bin/
/out/
*.dll
*.obj
*.pdb
*.lib
*.exp
*.ilk
*.idb

# ── IDE ──────────────────────────────────────────────────────────
/.vs/
.vscode/
.idea/
*.user
*.suo

# ── Runtime output (produced inside the game directory) ──────────
/logs/
/configs/

# ── OS noise ─────────────────────────────────────────────────────
Thumbs.db
.DS_Store

# ImGui writes imgui.ini next to the executable on shutdown; the overlay keeps settings in
# memory (fixed layout, no persistence yet), so the file is noise.
imgui.ini
imgui_*.ini
